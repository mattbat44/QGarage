[project]
name = "qhub"
version = "0.1.0"
description = "QHub - Modular App-Hosting Framework for QGIS"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=7.0",
]
